{
  "name": "goligoliapp",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "deploy": "wrangler pages deploy ./dist/ --project-name los-errores-de-nuestros-padres"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3"
  },
  "source": [
    "src/index.html"
  ]
}
